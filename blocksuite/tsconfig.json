{
  // exists for easier extending in blocksuite packages
  "extends": "../tsconfig.web.json",
  "compilerOptions": {
    "types": ["node"],
    "noEmit": false,
    "declaration": true,
    "emitDeclarationOnly": true
  }
}
